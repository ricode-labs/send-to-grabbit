{
  "name": "send-to-grabbit",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that sends browser downloads to Grabbit.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
